# Step by Step

## Create `pet-info` 

```bash
npx ng g c pets/pet-info --skip-tests --inline-style --inline-template --flat -m pets
```

## Create `pet-info-toys`

```bash
npx ng g c pets/pet-info-toys --skip-tests --inline-style --inline-template --flat -m pets
```

## Create `pet-edit`

```bash
npx ng g c pets/pet-edit --skip-tests --inline-style --inline-template --flat -m pets
```