import { Component, Input } from '@angular/core';

@Component({
  selector: 'app-basic',
  standalone: true,
  imports: [],
  template: `
    <div>
    </div>
  `,
  styles: ``
})
export class BasicComponent {
  // @Input() message!: string;
}
